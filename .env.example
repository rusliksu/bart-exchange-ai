# Database
DB_PASSWORD=changeme

# JWT secret (min 256 bits / 32+ chars)
JWT_SECRET=changeme-must-be-at-least-256-bits-long-for-hmac-sha

# OpenAI API key for AI recommendations (leave empty to use mock mode)
OPENAI_API_KEY=sk-your-key-here
